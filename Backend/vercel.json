{
    "version":2,
    "build":[
        {
            "src":"server.js",
            "use":"@vercel/model",
            "config":{
                "includeFiles":[
                    "dist/**"
                ]
            }
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"server.js"
        }
    ]
}